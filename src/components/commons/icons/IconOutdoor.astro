---
import IconKeenIcon from "./IconKeenIcon.astro";

interface Props {
  classNameContainer?: string | undefined;
  className?: string | undefined;
  color?: string | undefined;
  size?: string | undefined;
}

const {
  classNameContainer,
  className,
  color,
  size,
} = Astro.props;
---

<!-- ICON - COWORKING -->
<IconKeenIcon
  name="night-day"
  paths={10}
  classNameContainer={classNameContainer}
  className={className}
  color={color}
  size={size} />
